# ITERRET dependencies. Verified against Python 3.11.15 in a dedicated conda env
# (`conda create -n iterret python=3.11 && pip install -r requirements.txt`).

langgraph==1.2.5
openai==2.43.0
sentence-transformers>=3.0

# Optional: only needed for --surprise-segmentation (iterret.episode_segmenter /
# iterret.surprise). These pull in a local HuggingFace causal LM to compute token
# surprisal; a GPU is strongly recommended. The core pipeline runs without them.
torch>=2.1
transformers>=4.40