# Bytecode / build
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.so
*.whl

# Run artifacts -- NEVER commit these. outputs/ is 100s of MB of server logs
# and result checkpoints; it lives outside the repo by design.
outputs/
*.log
*.jsonl
delta-Mem/outputs/

# Model weights (multi-GB, redownloadable)
models/
delta-Mem/data/
delta-Mem/scripts/
delta-Mem/.git/

# OS / editor cruft
.DS_Store
*.bak
